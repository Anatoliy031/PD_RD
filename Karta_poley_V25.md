# Карта полей: АРМ ППО (V25) → PD_RD

Этап 0 · версия PD_RD 0.1.0 · 16.09.2026 · **на утверждении у Анатолия**

Документ фиксирует, откуда PD_RD берёт каждое исходное значение. После утверждения по нему
пишется импорт (этап «Модель, импорт, входной контроль»).

Источник — код `VOLS_ARM_V25` v3.5.10 (`ppo-core.js`, объект `ppo_vols_object_v2`)
и пилотный отчёт `ДРНУ-ППО/2026-23002603011239`.

## 1. Как устроены данные V25

Объект осмотра хранится одним JSON в localStorage под ключом `ppo_vols_object_v2`,
фотографии — в IndexedDB `ppo_vols_photos`. Разделы объекта:

| Раздел V25 | Содержимое |
|---|---|
| `pass` | Паспорт: словарь «код поля → значение» (`ОБЪЕКТ_НАИМ`, `ОТЧЁТ_НОМЕР`, `ВЕТЕР_РАЙОН` и др.) |
| `lines` | Линии: `line, disp, inv, year, kv, right, note` |
| `poles` | Опоры: `id, line, num, kv, mark, span, hangs, mest, angle, prevOwner, prevH, defect, defText, endPole, lat, lon, prevId/nextId, prevRef/nextRef, spanGeo, spanNext, spurOf, note, performer, term, cost` |
| `meas` | Измерения: `id, pole, line, kindId, name, place, value, unit, norm, verdict, proto, si, vals` |
| `si` | Средства измерений: `name, mark, sn, fgis, d1, d2` |
| `cables` | Размещаемые кабели: `mark, kv, h, arm, tens, note` |
| `wires` | Существующая подвеска: `mark, n, kv, h, tens, belong, note` |
| `acts` | Правки к мероприятиям (сами мероприятия V25 пересчитывает при каждой сборке) |
| `skips` | Метки KMZ, не ставшие опорами |

Резервная копия V25 (`ППО_осмотр_*.json`) — тот же объект плюс `photoStore` с фото в base64.
Это **основной вход** PD_RD, пока инструменты на разных адресах.

Производные значения (материал, тип, стойка, Mдоп, габаритный пролёт, состояние по ГОСТ,
технологическая возможность) V25 **не хранит**, а вычисляет из справочника опор
(`poleDerived`). PD_RD повторяет это по копии справочника `ppo-core-v3510.js`.

## 2. Соответствие полей

### 2.1 Паспорт и основание

| PD_RD `pdrd-project/1` | V25 | Примечание |
|---|---|---|
| `passport.object` | `pass.ОБЪЕКТ_НАИМ` | |
| `passport.place` | `pass.ОБЪЕКТ_МЕСТО` (+ `ОБЪЕКТ_РЕГИОН/РАЙОН/НП`) | |
| `passport.user` | `pass.ПОЛЬЗОВАТЕЛЬ_НАИМ` | Пилот: «ООО "СвязьстройТелеКом" в интересах ПАО "Ростелеком"» → разделить на пользователя и оператора (ручное подтверждение) |
| `passport.operator` | — | Вводится; предлагается из текста «в интересах …» |
| `passport.owner`, `branch` | `pass.ВЛАДЕЛЕЦ_НАИМ`, `ВЛАДЕЛЕЦ_ФИЛИАЛ` | |
| `basis.report13.number/date` | `pass.ОТЧЁТ_НОМЕР`, `ОТЧЁТ_ДАТА` | Обязательно |
| `basis.report13.sha256` | хэш входного файла | Контроль «отчёт не менялся после импорта» |
| `basis.request` | `pass.ЗАПРОС_НОМЕР`, `ЗАПРОС_ДАТА` | Для ПЗ |
| `basis.tz`, `basis.tu` | — | Вводятся (ТЗ оператора, ТУ владельца) |
| `legal.*`, `passport.signs.*`, шифр, марки | — | Только в PD_RD |

### 2.2 Климат

| PD_RD | V25 | Примечание |
|---|---|---|
| `climate.windRegion`, `windPa` | `pass.ВЕТЕР_РАЙОН` (+ давление) | Пилот: III, 650 Па |
| `climate.iceRegion`, `iceMm` | `pass.ГОЛОЛЁД_РАЙОН` (+ стенка) | Пилот: II, 15 мм |
| `climate.terrain` | `pass.ТИП_МЕСТНОСТИ` | Пилот: A |
| `climate.seismic` | `pass.СЕЙСМИКА` | Пилот: 7 |
| `climate.source` | `pass.КЛИМАТ_ИСТОЧНИК` | Автоопределение → `confirmed = false` |

Точные коды полей климата сверяются с `PASSPORT` при написании импорта.

### 2.3 Линии и провода

| PD_RD | V25 | Примечание |
|---|---|---|
| `lines[].name` | `lines[].line` или `poles[].line` | Пилот: лист «Линии» пуст → линии собираются из опор |
| `lines[].kv` | `lines[].kv` / `poles[].kv` | |
| `lines[].wires[]` | `wires[]` по `kv` | **Нельзя переносить «все классы»**: провода задаются по каждой линии. Пилот: 0,4 кВ — СИП (марка, сечение, тяжение запрашиваются), 10 кВ — АС 35/6.2 |
| `lines[].existing[]` | `poles[].prevOwner`, `prevH`, `hangs` | Ранее размещённые ОК |

### 2.4 Опоры

| PD_RD | V25 | Примечание |
|---|---|---|
| `poles[].coords` | `lat`, `lon` | Ключ объединения физической опоры (допуск 1,5 м) |
| `poles[].lines[]` | `{line, num}` каждой записи | Пилот: 909 записей → 898 опор, 9 совместных групп |
| `poles[].mark` | `mark` | Без марки — блок |
| `fromReport.kv` | `kv` | Без класса — блок |
| `fromReport.span`, соседи | `span`, `spanNext`, `prevId/nextId`, `endPole` | Пролёты V25 считаются по координатам |
| `fromReport.terrain` | `mest` | |
| `fromReport.angle` | `angle` | Нужен для угловых опор |
| `fromReport.defect`, `defText` | `defect`, `defText` | Категория A/Б/В |
| `fromReport.state`, `possibility` | вычисляется (`poleDerived`) | Аварийная / «нет возможности» → решение «размещать» запрещено |
| `fromReport.ref` (стойка, Mдоп, габаритный пролёт, типовой проект) | справочник `POLES` | См. раздел 4 |
| `fromReport.photos` | IndexedDB / `photoStore` | Только ссылки, фото — в `pdrd_media` по запросу |
| `design.*` | — | Решения PD_RD |

Поля `fromReport` доступны только для чтения. Несогласие с отчётом оформляется
замечанием входного контроля, а не правкой.

### 2.5 Кабель, измерения, приборы, мероприятия

| PD_RD | V25 | Примечание |
|---|---|---|
| `cable` (заявленный) | `cables[0]` + справочник `CABLES` | Пилот: ОС-М5-8-7,0; монтажное тяжение не задано → блок |
| `metrology[]` (исходные) | `meas[]`, `si[]` | Для ссылок в ПЗ; метрологический раздел ПД строится заново |
| `measuresE1[]` | мероприятия группы Е.1 (`buildActs`) | Исполнитель принудительно «пользователь» с отметкой |
| ограничения по опорам | мероприятия группы Е.2 | Не переносятся как работы пользователя |

## 3. Что берём из кода V25

| Узел | Решение |
|---|---|
| `buildDocx` (index.html) | Повторить подход: сырой OOXML + JSZip 3.10.1 с cdnjs. Вынести в `pdrd-docx.js`, добавить штампы форм 5/6 |
| `ppo-audit.js` + шлюз выпуска | Повторить механику: блок, обход с обоснованием ≥ 40 символов и ФИО |
| Справочники `POLES`, `CABLES`, `WIRES`, `VL` | Копия `ppo-core-v3510.js` без правок (этап импорта) |
| Хранилище с отметкой записи | Повторено в `pdrd-core.js` (ключи `pdrd_*`) |
| Leaflet с unpkg в `karta.html` | Не повторять: jsdelivr или локальная копия |

## 4. Замечания к справочникам V25

1. README V25 прямо указывает: **допустимые моменты опор, характеристики кабелей и климат —
   типовые, подтверждаются по типовым проектам**. В PD_RD каждое такое значение получает статус
   `требует подтверждения` до сверки с альбомом серии (3.407.1-136, 3.407.1-143) или паспортом.
2. Справочник кабелей V25 содержит обобщённые марки (ОКСН-6-2,7 и т. п.), а не конкретные
   изделия. Для ПД нужен каталог с паспортными данными — он утверждается позднее.
3. Справочник опор 35–110 кВ в V25 надо проверить на полноту (металлические решётчатые опоры).

## 5. Вопросы для утверждения

1. Разделение «пользователь / оператор» для пилота: пользователь — ООО «СвязьстройТелеКом»,
   оператор (заказчик сети) — ПАО «Ростелеком». Верно?
2. Основной вход до переезда на общий адрес — резервная копия V25 (`.json`). Согласны?
